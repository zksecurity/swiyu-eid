# Citation audit for the Swiyu transcript-privacy workshop paper

Prepared 2026-09-21. `claim-map.md` is the controlling ledger: cite a source only for the claim in its **Supports** column, and preserve the qualification in **Does not support**. `references.bib` contains verified metadata. `source-notes.md` records primary URLs, access dates, and local evidence anchors.

Recommended paper set: `oid4vp`, `sdjwt`, `swiyu`, `openac`, `epfl`, `metamorphic`, `declassification`, `hyperproperties`, and `goldwasser1989`. Add the remaining repository entries only where implementation provenance is discussed. Do not add zk.golf or better.codes: no paper claim currently needs them, and they do not substantiate the experiment.
