# swiyu Android wallet

Imported official Android holder application. Signed `x_swiyu_zkp` policies are parsed into a typed proof-runtime request, and the returned opaque proof uses the normal DCQL `vp_token` submission path. The native zkID runtime is the remaining mobile package.

```sh
./gradlew app:assembleProdRelease
```
