# Spartan2 backend

Rust adapter that loads Circom R1CS artifacts, generates circuit-specific keys, proves with Spartan2 and Hyrax, and verifies expected public values.

```sh
cargo test --lib --bins --no-default-features
```
