# Mobile proof bindings

Rust, Mopro, and Flutter bindings for running the zkID prover on Android and iOS. They are not yet connected to the imported swiyu Android wallet.

```sh
cargo build --release
```
