# zkID

Imported zkID proof implementation plus the age, canton, residence, and scoped-nullifier experiments in `wallet-unit-poc`.

See the [project overview](../../website/index.html#local-zk) for the architecture and the [benchmark index](../../benchmark/README.md) for measurements.
