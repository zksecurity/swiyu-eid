# Integration tests

Component tests cover the circuits, TypeScript profiles, Spartan backend, Java sidecar adapter, baseline controls, and nullifier concurrency. Verifier tests exercise the normal OID4VP submission endpoint. Android tests cover signed policy parsing, runtime-request construction, and opaque proof-envelope serialization into `vp_token`. A device-to-verifier test still requires the mobile proof runtime.
