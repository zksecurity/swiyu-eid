# Contracts

The Java verifier currently supports the monolithic age profile through `x_swiyu_zkp` and the local HTTP sidecar. The optimized profiles use the TypeScript Prepare/Show envelope and still need one shared mobile-to-verifier schema.
